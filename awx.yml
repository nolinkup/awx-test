---
  - hosts: localhost
    become: true
    become_method: sudo
    tasks:
      - name: Gib was aus
        debug: 
           msg: Ansible ist wunderbar "{{ ansible_hostname }}"
  - action: debug msg="Tolle Sache"